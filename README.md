## Test the service

http://localhost/api/

### Build th images

`$ docker-compose build`

### Start the containers

`$ docker-compose up -d`

### Stop & remove the containers

`$ docker-compose down`
